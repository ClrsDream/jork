package com.xiaoteng.jork.server.servers;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class HttpsServer implements Runnable {

    private final static Logger log = LogManager.getLogger(HttpsServer.class);

    private final static int PORT = 443;

    @Override
    public void run() {

    }
}
